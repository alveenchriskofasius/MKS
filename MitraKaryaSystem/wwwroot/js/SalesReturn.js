$(document).ready(()=> SalesReturnPage.Init());
const SalesReturnPage = {
  Init(){ this.Bind(); this.InitTables(); this.LoadForm(0); },
  // ================= BINDINGS =================
  Bind(){
    $('#srNew').off('click').on('click',()=> this.LoadForm(0));
    $('#srSave').off('click').on('click',()=> this.Save());
    $('#srSearch').off('click').on('click',()=> this.Search());
    $(document).off('change.srReturnType','input[name="srReturnType"]').on('change.srReturnType','input[name="srReturnType"]', ()=> this.ToggleReturnType());
    $(document).off('change.srRefund','#srRefundAmount').on('change.srRefund','#srRefundAmount', ()=> this.RefreshSummary());
    $(document).off('change.srROS','#srReturnOriginalToStock').on('change.srROS','#srReturnOriginalToStock', ()=> this.RefreshSummary());
    $(document).off('click.srAdd','#srAdd').on('click.srAdd','#srAdd', ()=> this.AddProduct(false));
    $(document).off('click.srAddRep','#srAddReplacement').on('click.srAddRep','#srAddReplacement', ()=> this.AddProduct(true));
    $(document).off('keypress.srProd','#srProduct').on('keypress.srProd','#srProduct', e=>{ if(e.which===13){ e.preventDefault(); this.AddProduct(false);} });
    $(document).off('keypress.srProdRep','#srProductReplacement').on('keypress.srProdRep','#srProductReplacement', e=>{ if(e.which===13){ e.preventDefault(); this.AddProduct(true);} });
    $(document).off('change.srLinked','#srLinkedMode').on('change.srLinked','#srLinkedMode', ()=> this.ToggleLinkedMode());
    $(document).off('click.pickSO','#btnPickSO').on('click.pickSO','#btnPickSO', ()=> this.OpenPickSO());
    $(document).off('click.clearSO','#btnClearSO').on('click.clearSO','#btnClearSO', ()=> this.ClearSO());
  },
  // ================= SO PICKER =================
  ToggleLinkedMode(){ if($('#srLinkedMode').is(':checked')) $('#srLinkedSelectWrapper').show(); else { $('#srLinkedSelectWrapper').hide(); this.ClearSO(); } },
  OpenPickSO(){ const modal = new bootstrap.Modal(document.getElementById('pickSOModal')); modal.show(); this.LoadSOGrid(); },
  LoadSOGrid(){ let tb=$('#tablePickSO').DataTable({ deferRender:true, destroy:true, searching:true, columns:[ {data:'id'}, {data:'no'}, {data:'date'}, {data:'amount', className:'text-end', render: $.fn.dataTable.render.number(',', '.',2)}, {data:null, orderable:false, render:()=> `<button class='btn btn-sm btn-primary so-pick'><i class='fa fa-check'></i></button>` } ]}); $.get('/SalesOrder/FillGrid', data=>{ let list = data.result || data; tb.clear().rows.add(list).draw(); }); $('#tablePickSO').off('click.soPick').on('click.soPick','.so-pick', function(){ const row = tb.row($(this).closest('tr')).data(); $('#srLinkedSOID').val(row.id); $('#srLinkedSONo').val(row.no); bootstrap.Modal.getInstance(document.getElementById('pickSOModal')).hide(); SalesReturnPage.LoadSalesOrderItems(row.id); $('#srLinkedMode').prop('checked', true); SalesReturnPage.ToggleLinkedMode(); }); },
  LoadSalesOrderItems(soId){ $.get('/SalesOrder/GetDetailListById',{ id: soId }, res=>{ let list = Array.isArray(res) ? res : (res && res.result ? res.result : []); if(!list || list.length===0){ toastr.warning('SO items not found'); return; } const tbl=$('#tableSalesReturnItems').DataTable(); tbl.clear(); list.forEach(item=> tbl.row.add({ id:0, productID: item.productID || item.ProductID, product: item.product || item.Product || item.productName || item.ProductName, quantity: 0, unitPrice: item.unitPrice || item.UnitPrice || 0, subTotal: 0, sourceSalesOrderItemID: item.id || item.ID, maxQty: item.remainingQty != null ? item.remainingQty : (item.quantity != null ? item.quantity : 0) })); tbl.draw(); this.RefreshPairingOptions(); this.RefreshSummary(); toastr.success('Sales Order loaded. Set qty for returned items.'); }); },
  ClearSO(){ $('#srLinkedSOID').val(''); $('#srLinkedSONo').val(''); },
  // ================= FORM LOAD =================
  LoadForm(id){ $('#srHeader').html('<div class="p-2 text-center"><div class="spinner-border spinner-border-sm"></div></div>'); $.get('/SalesReturn/FillForm',{id:id||0}, html=> { $('#srHeader').html(html); this.ToggleReturnType(); this.ToggleLinkedMode(); this.LoadDetails(id); }); },
  // ================= TABLE INIT =================
  InitTables(){ this.InitReturnedTable(); this.InitReplacementTable(); },
  InitReturnedTable(){ $('#tableSalesReturnItems').DataTable({ deferRender:true,destroy:true,searching:false,paging:false,info:false, columns:[ {data:'productID', visible:false}, {data:'product'}, {data:'quantity', className:'text-end', render:(d,t,r)=> t==='display'? this.RenderQtyInput(d,r): d}, {data:'unitPrice', className:'text-end', render: $.fn.dataTable.render.number(',', '.',2)}, {data:'subTotal', className:'text-end', render: $.fn.dataTable.render.number(',', '.',2)}, {data:null, orderable:false, render:()=> `<button class='btn btn-sm btn-danger sr-del'><i class='fa fa-trash'></i></button>` } ], data: [] }); const tbl=$('#tableSalesReturnItems'); tbl.off('change.qty').on('change.qty','input.sr-qty', e=>{ const t=$('#tableSalesReturnItems').DataTable(); let idx=t.cell($(e.target).closest('td')).index(); if(!idx) return; let row=t.row(idx.row).data(); let q=parseInt($(e.target).val(),10); if(isNaN(q)||q<0) q=0; const max=parseInt($(e.target).data('max')); if(!isNaN(max)&& q>max){ q=max; $(e.target).val(max); toastr.info('Exceeds max refundable qty'); } row.quantity=q; row.subTotal=row.unitPrice*q; t.row(idx.row).data(row).invalidate(); this.RefreshPairingOptions(); this.RefreshSummary(); }); tbl.off('click.del').on('click.del','.sr-del', function(){ const t=$('#tableSalesReturnItems').DataTable(); const r=t.row($(this).closest('tr')); const d=r.data(); if(d.id>0){ Swal.fire({title:'Delete item?',icon:'warning',showCancelButton:true}).then(rs=>{ if(rs.isConfirmed){ $.get('/SalesReturn/DeleteItem',{id:d.id},res=>{ if(res.success){ r.remove().draw(); SalesReturnPage.RefreshPairingOptions(); SalesReturnPage.RefreshSummary(); } else toastr.error(res.result||'Delete failed'); }); }}); } else { r.remove().draw(); SalesReturnPage.RefreshPairingOptions(); SalesReturnPage.RefreshSummary(); } }); },
  RenderQtyInput(val,row){ const max = row.maxQty != null ? row.maxQty : ''; return `<input type="number" class="form-control form-control-sm sr-qty" value="${val}" min="0" ${max!==''?`max='${max}'`:''} data-max='${max}' />`; },
  InitReplacementTable(){ $('#tableSalesReturnReplacement').DataTable({ deferRender:true,destroy:true,searching:false,paging:false,info:false, columns:[ {data:'productID', visible:false}, {data:'product'}, {data:'exchangeSourceItemID', orderable:false, render:(d,t)=> t==='display'? this.BuildSourceSelect(d): d}, {data:'quantity', className:'text-end', render:(d,t)=> t==='display'? `<input type="number" class="form-control form-control-sm sr-rqty" value='${d}' min='1' />`: d}, {data:'unitPrice', className:'text-end', render: $.fn.dataTable.render.number(',', '.',2)}, {data:'subTotal', className:'text-end', render: $.fn.dataTable.render.number(',', '.',2)}, {data:null, orderable:false, render:()=> `<button class='btn btn-sm btn-danger sr-rdel'><i class='fa fa-trash'></i></button>` } ], data: [] }); const tbl=$('#tableSalesReturnReplacement'); tbl.off('change.rqty').on('change.rqty','input.sr-rqty', e=>{ const t=$('#tableSalesReturnReplacement').DataTable(); let idx=t.cell($(e.target).closest('td')).index(); if(!idx) return; let row=t.row(idx.row).data(); let q=parseInt($(e.target).val(),10); if(isNaN(q)||q<1) q=1; row.quantity=q; row.subTotal=row.unitPrice*q; t.row(idx.row).data(row).invalidate(); this.RefreshSummary(); }); tbl.off('change.srcSel').on('change.srcSel','.sr-source-select', e=>{ const t=$('#tableSalesReturnReplacement').DataTable(); let tr=$(e.target).closest('tr'); let row=t.row(tr).data(); row.exchangeSourceItemID = $(e.target).val()? parseInt($(e.target).val(),10): null; t.row(tr).data(row).invalidate(); }); tbl.off('click.rdel').on('click.rdel','.sr-rdel', function(){ const t=$('#tableSalesReturnReplacement').DataTable(); const r=t.row($(this).closest('tr')); const d=r.data(); if(d.id>0){ Swal.fire({title:'Delete item?',icon:'warning',showCancelButton:true}).then(rs=>{ if(rs.isConfirmed){ $.get('/SalesReturn/DeleteItem',{id:d.id},res=>{ if(res.success){ r.remove().draw(); SalesReturnPage.RefreshSummary(); } else toastr.error(res.result||'Delete failed'); }); }}); } else { r.remove().draw(); SalesReturnPage.RefreshSummary(); } }); },
  BuildSourceSelect(selectedId){ const retTable=$('#tableSalesReturnItems').DataTable(); let opts='<option value="">- choose -</option>'; retTable.rows().every(function(){ let r=this.data(); if((r.quantity||0)>0){ const idCandidate = r.id || r.sourceSalesOrderItemID || ''; opts+=`<option value='${idCandidate}' ${(selectedId && selectedId==idCandidate)?'selected':''}>${r.product} (Qty ${r.quantity})</option>`; } }); return `<select class='form-select form-select-sm sr-source-select'>${opts}</select>`; },
  RefreshPairingOptions(){ const repl=$('#tableSalesReturnReplacement').DataTable(); repl.rows().every(function(){ $('#tableSalesReturnReplacement').DataTable().row(this).invalidate(); }); },
  // ================= DATA LOAD =================
  LoadDetails(id){ const ret=$('#tableSalesReturnItems').DataTable(); const rep=$('#tableSalesReturnReplacement').DataTable(); if(!id){ ret.clear().draw(); rep.clear().draw(); this.RefreshSummary(); return; } $.get('/SalesReturn/GetDetailList',{id:id}, res=>{ ret.clear(); rep.clear(); (res||[]).forEach(r=>{ r.subTotal = r.quantity * r.unitPrice; if(r.isReplacement) rep.row.add(r); else ret.row.add(r); }); ret.draw(); rep.draw(); this.RefreshPairingOptions(); this.RefreshSummary(); }); },
  // ================= ADD PRODUCT =================
  AddProduct(isReplacement){ if($('#srLinkedMode').is(':checked') && !isReplacement){ return toastr.info('Returned items berasal dari Sales Order. Gunakan qty kolom.'); } const input = isReplacement ? '#srProductReplacement' : '#srProduct'; const name = $(input).val(); if(!name) return toastr.info('Type product'); $.get('/Product/GetProductComboList',{ name:name }, data=>{ if(!data||!data.result||data.result.length===0) return toastr.warning('Not found'); const prod=data.result[0]; const table = isReplacement ? $('#tableSalesReturnReplacement').DataTable() : $('#tableSalesReturnItems').DataTable(); let exists=false; table.rows().every(function(){ let r=this.data(); if(r.productID==prod.id){ exists=true; r.quantity+=1; r.subTotal=r.quantity*r.unitPrice; table.row(this).data(r).invalidate(); }}); if(!exists){ const baseRow = { id:0, productID:prod.id, product:prod.name, quantity:1, unitPrice:prod.unitPrice, subTotal:prod.unitPrice, isReplacement:isReplacement }; if(isReplacement){ baseRow.exchangeSourceItemID = null; } table.row.add(baseRow).draw(); } $(input).val('').focus(); if(!isReplacement) this.RefreshPairingOptions(); this.RefreshSummary(); }); },
  // ================= COLLECT & SAVE =================
  Collect(){ const ret=$('#tableSalesReturnItems').DataTable(); const details=[]; ret.rows().every(function(){ let r=this.data(); if((r.quantity||0)>0) details.push({ ID:r.id||0, ProductID:r.productID, Quantity:r.quantity, UnitPrice:r.unitPrice, IsReplacement:false, SourceSalesOrderItemID:r.sourceSalesOrderItemID }); }); const rep=$('#tableSalesReturnReplacement').DataTable(); const repl=[]; rep.rows().every(function(){ let r=this.data(); repl.push({ ID:r.id||0, ProductID:r.productID, Quantity:r.quantity, UnitPrice:r.unitPrice, IsReplacement:true, ExchangeSourceItemID:r.exchangeSourceItemID }); }); return { details, repl }; },
  Save(){ const c=this.Collect(); const type=$('input[name="srReturnType"]:checked').val(); if(type==='Refund'){ if(c.details.length===0){ toastr.info('Masukkan minimal 1 item yang diretur'); return; } } else if(type==='Exchange'){ if(c.details.length===0){ toastr.info('Exchange perlu returned items'); return; } if(c.repl.length===0){ toastr.info('Exchange perlu replacement items'); return; } // optional pairing validation
    const unpaired = c.repl.some(x=> !x.ExchangeSourceItemID); if(unpaired){ toastr.info('Semua replacement harus dipair ke returned'); return; } }
    let model={ ID:$('#srID').val(), Date:$('#srDate').val(), No:$('#srNo').val(), CustomerID:$('#srCustomerID').val(), Note:$('#srNote').val(), ReturnType:type, ReturnOriginalToStock:$('#srReturnOriginalToStock').is(':checked'), RefundAmount:$('#srRefundAmount').val(), IsLinked:$('#srLinkedMode').is(':checked'), LinkedSalesOrderID: $('#srLinkedSOID').val(), Details:c.details, ReplacementDetails:c.repl };
    $('#srSave').prop('disabled',true); $('#srSave .spinner-border').removeClass('d-none'); $.ajax({url:'/SalesReturn/Save', type:'POST', data:model}).done(res=>{ if(res.success){ toastr.success('Saved'); $('#srID').val(res.id); $('#srNo').val(res.no); if(res.netDifference!==undefined){ $('#lblSummaryNet').text(parseFloat(res.netDifference).toFixed(2)); const note = res.netDifference>0? 'Customer Pays' : (res.netDifference<0? 'Refund to Customer':'Even'); $('#lblSummaryNetNote').text(note); } } else toastr.error(res.result||'Save failed'); }).fail(err=> toastr.error(err.responseText||'Error')).always(()=>{ $('#srSave').prop('disabled',false); $('#srSave .spinner-border').addClass('d-none'); });
  },
  // ================= SUMMARY =================
  ToggleReturnType(){ const type=$('input[name="srReturnType"]:checked').val(); if(type==='Exchange'){ $('#srExchangePanel').removeClass('d-none'); $('#rowSummaryReplacement,#rowSummaryNet').removeClass('d-none'); $('#rowSummaryRefund').addClass('d-none'); } else { // Refund path -> clear replacement table
      const rep=$('#tableSalesReturnReplacement').DataTable(); rep.clear().draw(); $('#srExchangePanel').addClass('d-none'); $('#rowSummaryReplacement,#rowSummaryNet').addClass('d-none'); $('#rowSummaryRefund').removeClass('d-none'); }
    this.RefreshSummary(); },
  RefreshSummary(){ const ret=$('#tableSalesReturnItems').DataTable(); let totalReturned=0; ret.rows().every(function(){ let r=this.data(); totalReturned += (r.quantity * r.unitPrice); }); const rep=$('#tableSalesReturnReplacement').DataTable(); let totalReplacement=0; rep.rows().every(function(){ let r=this.data(); totalReplacement += (r.quantity * r.unitPrice); }); $('#srTotal').text(totalReturned.toFixed(2)); $('#srReplacementTotal').text(totalReplacement.toFixed(2)); $('#lblSummaryReturned').text(totalReturned.toFixed(2)); $('#lblSummaryReplacement').text(totalReplacement.toFixed(2)); const type=$('input[name="srReturnType"]:checked').val(); if(type==='Exchange'){ const net= totalReplacement-totalReturned; $('#lblSummaryNet').text(net.toFixed(2)); $('#lblSummaryNetNote').text(net>0? 'Customer Pays': (net<0? 'Refund to Customer':'Even')); } else { const refundInput = parseFloat($('#srRefundAmount').val())||0; $('#lblSummaryRefund').text(refundInput.toFixed(2)); } },
  // ================= SEARCH =================
  Search(){ const modal=new bootstrap.Modal(document.getElementById('srSearchModal')); modal.show(); let tb=$('#tableSalesReturnSearch').DataTable({ deferRender:true,destroy:true,searching:false, columns:[ {data:'no'},{data:'date'},{data:'customer'},{data:'amount', className:'text-end', render: $.fn.dataTable.render.number(',', '.',2)}, {data:null, orderable:false, render:()=> `<button class='btn btn-sm btn-primary sr-edit'><i class='fa fa-edit'></i></button> <button class='btn btn-sm btn-danger sr-del2'><i class='fa fa-trash'></i></button>` } ]}); $.get('/SalesReturn/GetSearchList', data=>{ tb.clear().rows.add(data).draw(); }); $('#tableSalesReturnSearch').off('click.edit').on('click.edit','.sr-edit', function(){ let row=tb.row($(this).closest('tr')).data(); SalesReturnPage.LoadForm(row.id); modal.hide(); }); $('#tableSalesReturnSearch').off('click.del').on('click.del','.sr-del2', function(){ let row=tb.row($(this).closest('tr')).data(); Swal.fire({title:'Delete?',icon:'warning',showCancelButton:true}).then(r=>{ if(r.isConfirmed){ $.get('/SalesReturn/Delete',{id:row.id}, res=>{ if(res.success){ toastr.success('Deleted'); SalesReturnPage.LoadForm(0); SalesReturnPage.Search(); } else toastr.error(res.result||'Delete failed'); }); }}); }); }
};
